const mongoose= require('mongoose')

const productdata= new mongoose.Schema({
    name: {
        type: String,
        required: true
    },
    price: {
        type: Number,
        required: true
    },
    description: {
        type: String,
        required: true
    }
})

mongoose.model('product', productdata )